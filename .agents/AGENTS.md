# Custom Rules for Tijario Android

- **NEVER delete, modify, or reset any files (including configuration files like `gradle.properties`, `local.properties`, or source files) without the user's explicit permission.**
- Always ask and confirm before making destructive changes or discarding local changes.
